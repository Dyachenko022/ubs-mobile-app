import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  swipeOut: {
    // flex: 1,
    height: '100%',
    width: 80,
    alignItems: 'center',
    justifyContent: 'center'
  },

  swipeOutText: {
    color: '#fff',
    fontSize: 10,
  }
});